using System.Web.Mvc;

namespace PizzaWebApp
{
    public class FilterConfig
    {
        public static void RegisterGlobalFilters(GlobalFilterCollection filters)
        {
            //filters.Add(new LogAttribute());
            //filters.Add(new Log1Attribute());
        }
    }
}
